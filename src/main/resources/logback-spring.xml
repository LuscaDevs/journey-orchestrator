<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Application name -->
    <property name="APP_NAME" value="journey-orchestrator"/>

    <!-- Console appender for human-readable logging -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} correlationId=%X{correlationId:-none} method=%X{httpMethod:-none} path=%X{requestPath:-none} errorCode=%X{errorCode:-none} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root log level with console output -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

    <!-- Logger configurations -->
    <logger name="com.luscadevs.journeyorchestrator" level="DEBUG"/>
    <logger name="org.springframework" level="INFO"/>
    <logger name="org.springframework.web" level="INFO"/>
    <logger name="org.springframework.data.mongodb" level="WARN"/>
    <logger name="org.mongodb.driver" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
</configuration>